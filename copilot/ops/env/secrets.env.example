# Secrets for the paper VM's services. Copy to ~/.config/copilot/secrets.env, chmod 600.
# Values come from the password manager. This file is a list of names, never of values.

MARKETSTACK_API_KEY=
DATABENTO_API_KEY=
PUSHOVER_TOKEN=
PUSHOVER_USER_KEY=
