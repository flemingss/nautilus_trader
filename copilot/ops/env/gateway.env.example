# The Gateway container's login. Copy to ~/.config/copilot/gateway.env, chmod 600.
# The password goes in ~/.config/copilot/tws_password (chmod 600), read as a compose secret.

TWS_USERID=
# Set to enable the VNC screen, reached through an SSH tunnel to 127.0.0.1:5900.
VNC_SERVER_PASSWORD=
